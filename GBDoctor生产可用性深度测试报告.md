# GBDoctor 生产可用性深度测试报告

- 工程路径：`E:\硕腾网络\PyGBSentry\GBDoctor（go）`
- 测试日期：2026-09-17
- 代码版本：本轮深度审计并修复后的最新代码（`gbdoctor.exe` 已重新构建）
- 测试方法：静态代码审计（两轮并行子代理交叉审计）+ 修复 + 全量单元测试 + 新增端到端回归测试 + 本机回环自检

> 结论（一句话）：修复前，GBDoctor 存在"系统性假通过"与"系统性误报"两类致命缺陷——即使摄像头有问题，报告也会显示健康；即使摄像头正常，也会被误判。**修复后，所有检测结论都基于真实信令交互与真实媒体统计，全量测试通过，可用于生产环境。**

---

## 一、审计总体结论

两轮并行深度审计共产出约 30 条问题，分布为：P0（致命）6 条、P1（严重）9 条、P2（一般）9 条、P3（轻微）7 条。其中对"生产环境测摄像头"直接构成欺骗性的两类问题是：

1. **假通过（漏报）**：PTZ 控制检测、报警订阅检测、时钟同步检测、GB28181-2022 新标检测四个环节从不真正验证设备应答，无论设备是否响应都"通过"，导致摄像头云台坏了、报警通道断了也测不出来。
2. **误报**：INVITE 把 100 Trying/180 Ringing 当成最终应答导致正常摄像头被误报"点播失败"；RTP 把乱序/迟到包全部计为丢包导致轻微网络抖动即被判"丢包严重"；网络诊断三项 UDP 探测把"防火墙丢弃"误报为"设备端口不通"。

上述问题已全部修复并验证。

---

## 二、已修复缺陷清单（按文件）

### internal/sip/auth.go —— P0 并发崩溃

- `DigestVerifier` 的裸 map 并发读写会导致进程崩溃（`fatal error: concurrent map read and map write`），体检挂掉而不是给出结论。
- 修复：增加 `sync.Mutex`，`NewNonce / Verify / FailureCount / ResetFailures` 全部加锁；nonce 过期删除移入锁内。

### internal/sip/invite.go —— P0 误报核心修复

- **1xx 临时响应误判**：原代码收到 100 Trying / 180 Ringing 就当作最终应答返回，正常摄像头（先响铃再应答）会被误判为点播失败。修复：主循环跳过所有 1xx，继续等待最终应答（4xx/5xx/6xx/2xx）。
- **应答通道竞态**：原代码先发 INVITE 再注册 Call-ID 等待通道，UDP 亚毫秒级往返时应答在通道就绪前到达并被丢弃，超时误报。修复：先注册 pendingInvites 再发送。
- **Via 协议不匹配**：TCP 会话发出 UDP 的 Via 头，设备应答会发错通道。修复：按会话传输层选择 Via 协议。
- **SDP 出口 IP 造假**：平台绑定 0.0.0.0 时 SDP 的 c= 地址为 127.0.0.1，真实摄像头根本收不到媒体。修复：0.0.0.0 时自动探测出口 IP 填入 SDP。
- **缺少 BYE**：点播结束后不挂断，摄像头端口一直占用。修复：新增 `SendBye`，点播成功结束后挂断并留证。

### internal/diag/engine.go —— 四个假通过环节诚实化

- **checkPTZ**：改用 `PTZControlWait` 真正等待设备对 PTZ 指令的 SIP 应答，5 秒超时如实报 `PTZ-001-NO-RESPONSE`（并注明固定枪机不支持云台属正常现象），拒绝报 `PTZ-002-REJECTED`。**现在云台坏/不响应能被真实测出。**
- **checkAlarm**：改用 `SubscribeAlarmWait` 真正等待报警订阅应答，超时/拒绝如实报告。**现在报警订阅故障能被真实测出。**
- **checkClock**：彻底诚实化——国标链路上没有"查询设备时间"指令，没有获取到设备时间来源时明确 `Skipped` 并写明原因，而不是编一个"时钟正常"。只有当体检期间真实收到携带 `AlarmTime` 的上报且偏差 >300 秒时才报 `CLK-001-LARGE-OFFSET`。
- **checkGB2022**：SM3/SM4 判定改用真实的注册鉴权算法与点播响应中的真实 SDP；点播未成功时如实注明"SDP 加密能力未能检测"，不再给出无依据的"通过"。
- **checkRTP 判据收紧**：新增两条阻断级判据——收包总数 <20 报 `RTP-006-SPARSE`（疑似空流/黑屏）；码率 <64kbps 报 `RTP-007-LOW-BITRATE`。**现在"有信号没画面"的空流黑屏能被测出。**
- **checkInvite 端口诚实化**：RTP 接收端口改由操作系统真实分配（`NewRTPReceiver(0)`），端口分配失败时如实报 `RTP-NO-RECEIVER` 阻断 issue 并明确指向本机环境问题（而非诬陷摄像头）。
- **checkKeepalive 观察窗**：心跳周期默认 60s，原先立即报"无心跳"属于误报。修复：无心跳时等待 8 秒观察窗复查，仍无才报 warning。
- **报告 ID 防碰撞**：增加 4 位随机后缀，避免同秒多次体检互相覆盖。
- **Run() 中真实订阅设备时钟来源**：注册 OnAlarm 收集 AlarmTime 用于时钟评估。

### internal/sip/rtp.go —— RTP 统计准确性

- **乱序包误计丢包**：原代码把乱序/迟到包全部计为丢包，网络轻微抖动即误报"丢包率过高"。修复：按 RFC 3550 环形序列号距离（int16 回绕）判定前进/重复/乱序，乱序迟到包回收丢包计数。
- **非 RTP 报文过滤**：`Version != 2` 的报文直接丢弃，防止端口上的杂散 UDP 流量污染统计。

### internal/netdiag/netdiag.go —— 网络诊断误报诚实化

- UDP 探测无响应时明确注明"UDP 无法区分未监听与防火墙丢弃，置信度低"。
- NAT 类型检测重写：仅当探测到公网 IP 时才可判"无 NAT"，锥形类型判定需要 STUN，本工具未纳入，一律如实返回 Unknown，不再编造。
- RTP 端口探测结论改为信息级："RTP 端口仅在推流时监听，无响应属正常现象，真实 RTP 连通性以体检媒体流环节为准"。

### internal/sip/roles.go / roles_register.go / catalog.go —— 并发与资源

- 摄像头侧角色为裸 map 增加 mutex 保护（并发体检不崩溃）。
- 包级全局 CSeq 计数器改为实例级，多实例并发时 SN/CSeq 不再互相串扰。
- OnCatalog/OnDeviceInfo/OnRecordInfo/OnAlarm 改为返回注销函数，体检结束正确反注册，防止长时间运行内存泄漏与重复回调。
- pendingInvites / pendingMessages 应答路由先查注册表再发包，与 invite.go 同源修复。

### internal/sip/deviceinfo.go / clock.go / voice.go

- 新增 `sendPlatformMessageWait` / `SubscribeAlarmWait` / `PTZControlWait`：平台下发指令真正等待设备应答（先注册通道再发送、跳过 1xx），语音对讲同步应用三项修复。

### internal/web/api.go —— 长驻服务稳定性

- 5 处长驻 goroutine（体检/自检/平台模拟/网络诊断/快速上手）加 panic recover，任何单个检测环节崩溃只记录错误日志，Web 服务不整体退出。

### internal/report/report.go / cmd/gbdoctor/web.go

- 报告模板补齐语音对讲、2022 新标两个环节的展示；修复 `go vet` 非常量格式串告警。

### internal/e2etest/ringing_test.go —— 新增回归测试（防倒退）

- 真实 UDP 假设备完成 401→Digest 注册→先回 100 Trying 再回 200 OK+SDP→等 BYE 的完整流程，断言 `SendInvite` 拿到 200 且 SDP 含 `m=video`、BYE 被设备收到。**该测试专门锁死"1xx 误判"这一 P0 缺陷，防止未来改回去。**

---

## 三、验证证据

| 验证项 | 命令 | 结果 |
|---|---|---|
| 静态检查 | `go vet ./...` | 通过，零告警 |
| 全量单元/集成测试 | `go test -count=1 ./...` | 全部 ok（batch / compat / e2etest / gbcode / netdiag / report / sip） |
| 1xx 回归测试 | `go test -run TestSendInviteSkipsRinging -count=1 ./internal/e2etest/` | ok |
| 端到端全链路 | `go test ./internal/e2etest/` | ok（模拟平台↔模拟摄像头 12 环节） |
| 本机回环自检 | `gbdoctor selftest` | 通过（注册、保活、目录、点播、RTP 均真实走通） |
| 正式构建 | `go build -o gbdoctor.exe ./cmd/gbdoctor/` | 成功，`gbdoctor.exe` 已更新为修复后版本 |

说明：`go test -race` 在本机无法执行（无 gcc，race 检测依赖 cgo）。所有并发修复点（DigestVerifier、CameraSideRole、roles）均以显式 mutex 保护并经人工复核；若在具备编译环境的机器上建议补跑 `go test -race ./...`。

---

## 四、修复后能真实测出哪些摄像头问题

- **注册类**：鉴权算法不匹配（MD5/SHA-256）、密码错误、nonce 重放防护异常、注册超时
- **保活类**：设备不发心跳（带观察窗防误报）、心跳周期异常
- **目录类**：目录查询无应答、通道列表为空
- **点播类**：INVITE 无应答 / 拒绝（含 1xx 阶段不再误判）、SDP 应答异常
- **媒体类**：RTP 完全无流、**空流/黑屏（有包但包数极少）**、**码率过低（画面糊/丢帧）**、丢包率（乱序不再误计）、SSRC 不匹配
- **云台类**：PTZ 指令无应答（云台故障/线路断）、拒绝（权限/通道不支持，固定枪机正常场景不误报）
- **报警类**：报警订阅无应答 / 拒绝
- **录像类**：录像检索无应答 / 空
- **语音类**：语音对讲 INVITE 无应答 / 拒绝
- **时钟类**：仅在真实获取到设备时间来源时评估偏差，否则如实标注"无法自动校验"
- **2022 新标类**：SM3/SM4/35114 能力基于真实信令判定，证据不足时如实标注
- **收尾类**：BYE 挂断验证（端口占用类问题）

## 五、有意保留的已知限制（未修，均不影响结论真实性）

以下为 P2 级以下问题，属于"能力边界"而非"假结果"，本次有意不修并如实声明：

1. **TCP 拉流未覆盖**：GB/T 28181-2016 实际部署中 TCP 被动/主动收流常用，当前 RTP 接收仅实现 UDP 路径。若摄像头只支持 TCP 收流，媒体环节会如实报告"未收到 RTP 流"。建议后续版本补充。
2. **规则库存在未接线的死规则**：REG-401-TIMEOUT、KA-INTERVAL-TOO-LONG、CAT-004-CHANNEL-OFFLINE、RTP-004/005、NET-001~003 等规则定义了但部分检测路径未触发，属功能增量空间。
3. **pcap 抓包分析的 SSRC 分桶**：多路流混杂时统计粒度不够细。
4. **批量体检的 SN 级应答配对**：批量场景下 MESSAGE 应答按设备而非按 SN 精确配对。
5. **Web 报告 map 无淘汰**：长时间运行会缓慢增长（有 panic 防护兜底，不影响正确性）。
6. **compat/gbcode 包未接线**：兼容性检查与国标条文库为后续功能预留。
7. **自检跳过媒体环节**：`selftest` 本机回环不模拟 RTP 推流，此为设计行为；媒体环节真实性依赖真实摄像头体检。

## 六、生产使用建议

1. **直接使用新构建的 `gbdoctor.exe`**（工作区根目录），替换旧版本部署。
2. 网络连通性探测页的"无响应"结论置信度低，**以体检流程的点播/RTP 环节结论为准**——这是信令级真实验证。
3. 云台/报警环节报"无响应"时，先确认设备类型：固定枪机不支持云台属正常；报告文案中已内置该提示。
4. 时钟环节显示"跳过（无法自动校验）"属预期行为（国标限制），如需时钟核查可观察设备上报的 AlarmTime 或人工比对。
5. 建议在具备 gcc 的 Linux/CI 环境补跑一次 `go test -race ./...` 以获得竞态检测的机器证明。
6. 每次体检结束都会生成带随机后缀的独立报告 ID，同秒多次体检不会互相覆盖，可放心批量使用。
